// Not a secret: a Firebase web config is public by design, and Firestore is
// guarded by the rules in firestore.rules rather than by hiding these values.
//
// Copy them out of the Firebase console: Project settings -> Your apps -> Web.
export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.firebasestorage.app",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};
